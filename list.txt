Others:

- For enpassant we only have to store file, since file + turn give us the position -> more space in array[INFO] (which we do not need)
- Load images before running, instead of reloading them during running
- Switch eval system to bits instead of 'regular' computation
- Remove promotion flag from move to save space and solve this how???

Bugs:
- enpassant

REWRITE GUI (WITH UNDO CAPABILITIES)